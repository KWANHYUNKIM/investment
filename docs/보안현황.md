# 보안 현황 — 무엇이 되어 있고 무엇이 안 되어 있나

주장이 아니라 **실측**이다. 각 항목은 DB 에 물어보거나 코드를 열어 확인한 것이고,
자동 검증이 있는 것은 그 파일을 적어 뒀다.

갱신 2026-08-20 · 검증 `test_identity_security.py` · `test_db_schema.py` · `test_backup_crypt.py`

---

## 1. 되어 있는 것

| 항목 | 상태 | 검증 |
|---|---|---|
| 비밀번호 해시 | PBKDF2-HMAC-SHA256 **600,000회**, 사용자별 16B 솔트, 32B 해시. 평문 없음 | `test_password_hashing_meets_current_guidance` |
| 반복수 이행 | 로그인 성공 시 옛 해시를 조용히 재해시 — 재설정 없이 넘어간다 | 실측 확인 |
| 아이디 존재 노출 | 없는 아이디에도 같은 시간을 쓴다(타이밍 차이 제거) | 코드 |
| 자격증명 격리 | 앱 역할은 `identity.user_credential` 을 **직접 못 읽는다.** 로그인 함수 하나만 실행 가능 | `test_app_role_cannot_read_credentials_directly` |
| 전량 덤프 차단 | 로그인 함수는 아이디당 **한 행**만 반환. `%`·빈값·인젝션 문자열로 안 샌다 | `test_login_function_does_not_leak_other_accounts` |
| 남의 비밀번호 변경 | 함수 안에서 차단 | `test_cannot_change_someone_elses_password` |
| 사용자 목록 | 컨텍스트 없으면 0명 — 전량 조회 불가 | `test_user_list_is_not_readable_wholesale` |
| 행 수준 보안 | 가계부·포트폴리오·계정 — 남의 행이 SELECT 결과에서 사라짐 | `test_row_level_security_hides_other_users_rows` |
| 실패 시 방향 | 사용자를 못 정하면 **아무것도 안 보이는 쪽**으로 실패 | `test_no_current_user_means_no_rows` |
| 감사기록 | append-only. 트리거 + 권한으로 이중 차단 | `test_audit_events_cannot_be_modified` |
| 앱 최소권한 | `app_rw` 로 동작. DDL 은 소유자만 | `session.py` |
| 문장 타임아웃 | `statement_timeout 30s`, `idle_in_transaction 60s` | `session.py` |

### RLS 를 켤 때 두 번 틀렸던 것 (기록)

1. **슈퍼유저는 RLS 를 통째로 우회한다.** `FORCE` 도 못 막는다. Docker 의
   `POSTGRES_USER` 가 슈퍼유저라 그 계정으로 붙는 한 정책은 장식이었다.
2. **커넥션 풀에서 `SET ROLE` 이 풀린다.** 접속 시점에 걸면 재사용될 때 풀려 두 번째
   요청부터 무력해진다. 반대로 앞 요청의 정체성이 다음 요청으로 새기도 한다.

→ 둘 다 **트랜잭션 안에서 `SET LOCAL`**(`bind_request_context`)로 해결. 켠 줄 알고
있어서 더 위험한 종류라, 검증이 없었으면 그대로 배포됐을 것이다.

---

## 2. 안 되어 있는 것 — 우선순위 순

### 2.1 ~~계정이 두 곳에 있다~~ → **해결** (2026-08-20)

로그인이 DB 를 읽는다. 계정 조작마다 좁은 통로(SECURITY DEFINER 함수)만 열려 있어
전 계정 해시를 한 번에 가져오는 질의 자체가 없다.

| 동작 | 통로 | 노출되는 것 |
|---|---|---|
| 로그인 | `credential_for_login` | 아이디 하나당 한 행 |
| 아이디 중복 확인 | `username_taken` | 불리언 하나 |
| 아이디 찾기 | `find_usernames_by_email` | 이메일이 맞을 때만 |
| 비밀번호 재설정 | `reset_credential` | 아이디·이메일 쌍이 맞을 때만 |
| 관리자 목록 | `list_accounts` | 해시 없음 |

전환 검증: admin 의 솔트·해시·반복수가 파일과 **완전히 일치**(기존 비밀번호 그대로
로그인됨), 새 계정 가입→로그인→토큰→아이디찾기→비번변경 전 과정 통과, 옛 반복수
계정이 로그인 시 600,000회로 자동 갱신.

**토큰 서명 키를 DB 밖으로 뺐다.** `AUTH_SECRET` 환경변수를 쓴다 — DB 가 뚫렸을 때
해시는 못 풀어도 서명 키가 같이 나가면 아무 계정으로나 토큰을 위조할 수 있다.
비워 두면 기존 파일 값을 쓰므로 설정 전에도 로그인이 깨지지 않는다.

남은 것: `data/auth.json` 파일 삭제. 되돌릴 길로 아직 남겨 뒀다.

### 2.2 민감 데이터가 평문이다 (높음, 단 전면 암호화는 답이 아니다)

```
가맹점명  '성일마크사', '갓텐익스프레스 스타필드하남점'
거래금액  27000.00
이메일    admin@test.local
연소득    40000000.00
```

**"전부 암호화"는 실제로 못 한다.** 가맹점명은 검색하고 금액은 합계·정렬해야 하는데,
컬럼을 암호화하면 그 기능이 통째로 죽는다. 이건 게으름이 아니라 트레이드오프다.

계층을 나눠야 한다.

| 데이터 | 수단 | 이유 |
|---|---|---|
| 가맹점명·금액 | 접근통제(RLS) + **저장소 암호화** | 검색·집계가 핵심 기능 |
| 이메일 | 컬럼 암호화 + 조회용 해시 인덱스 | 동등 비교만 하면 됨 |
| 연소득·자산 | 컬럼 암호화 검토 | 집계 대상이 아님 |
| 비밀번호 | 이미 해시 | 복원할 이유가 없음 |

저장소 암호화는 볼륨/디스크 단에서 한다 — Windows BitLocker, 운영은 RDS/Cloud SQL 의
저장 암호화. `pgcrypto` 는 설치 가능한 상태로 확인해 뒀다.

### 2.3 DB 연결이 평문이다 (외부 배포 시 치명적)

```
서버 SSL 켜짐: off / 이 연결 SSL: False
```

로컬 루프백이라 당장 위험은 낮지만, DB 가 다른 호스트로 가는 순간 자격증명과 전 데이터가
네트워크에 그대로 흐른다. → 운영에서는 `sslmode=verify-full` 필수.

### 2.4 비밀·키가 평문 파일에 있다

`backend/.env` 에 DB 비밀번호, IMAP 비밀번호, DART·Finnhub·네이버·ECOS 키가 평문이다.
git 에는 안 올라가지만 디스크에는 그대로다.

→ 외부 배포 시 환경변수 주입 또는 비밀 관리자(AWS Secrets Manager 등). 로컬 단독
사용이면 디스크 암호화로 갈음.

### 2.5 그 밖

- ~~백업 없음~~ · ~~덤프 암호화~~ -> **해결**: `ops/win/backup.ps1`
  - 뜨고 나서 `pg_restore --list` 로 **열어서 확인**한다 — 뜨기만 하고 안 열어본
    덤프는 정작 필요한 날에 깨져 있는 경우가 흔하다
  - **AES-256-GCM 으로 암호화**하고 평문을 지운다. 인증 암호라 복호화할 때 변조가
    함께 걸린다(CBC 였다면 조작된 암호문도 조용히 복호화된다). 논스는 매번 새로
    뽑는다 — GCM 에서 논스를 재사용하면 **암호가 통째로 깨진다**
  - 암호화는 **검증 뒤에** 한다. 순서를 바꾸면 깨진 덤프를 암호화해 두고 멀쩡한 줄 안다
  - 키는 `BACKUP_KEY`(환경변수 -> `backend/.env`). **키를 잃으면 그 백업도 잃는다**
  - 보관 기간이 지나도 최소 3개는 남긴다. `data/backups/` 는 gitignore
  - **복원 훈련을 실제로 했다**: 암호화본 -> 복호화 -> 새 DB 에 `pg_restore` ->
    거래 60 · 종목 900 · 관심도 2,101 · 자격증명 1 전부 복원 확인
  - 검증 `tests/test_backup_crypt.py` — 왕복·변조탐지·논스 재사용 금지·키 부재 시 실패
  - **정기 실행**: `ops/win/schedule.ps1 install` 이 `Investment-Backup` 을 매일 02:40 에
    건다. 야간 배치(03:10)보다 **앞선다** — 배치가 데이터를 망가뜨렸을 때 그 직전
    상태가 남아 있어야 되돌릴 수 있다
- ~~의존성 취약점 스캔 없음~~ -> **해결**: `pip-audit`(`requirements-dev.txt`)
  - 처음 돌렸을 때 **36건 / 5개 패키지**. 그중 둘이 직접적인 위험이었다 —
    `starlette`(웹 계층 그 자체)와 `pypdf`(**신뢰할 수 없는 PDF** 인 카드 명세서를
    파싱하는 곳)
  - fastapi 0.115->0.141(starlette 0.41->1.6) · pypdf 6.14->6.16 · pillow · pyarrow ·
    setuptools 올림. 메이저 변경이라 서버 기동과 전 엔드포인트를 다시 확인했다
  - **프런트도 봤다**: `npm audit` 에 high 6건(next 안의 postcss 경로순회, sharp/libvips).
    next 16.2.9 -> 16.3.1 + `npm audit fix` 로 **0건**
  - **자동화**: `ops/win/audit.ps1`(파이썬+프런트, 종료코드로 CI 에 물릴 수 있음),
    `ops/git-hooks/pre-push`(테스트 + 감사). 설치 `git config core.hooksPath ops/git-hooks`
  - pre-commit 이 아니라 **pre-push** 인 이유: 커밋은 자주·중간 상태로도 한다.
    거기서 몇십 초 잡으면 `--no-verify` 가 습관이 되고 훅이 있으나 마나가 된다
  - 지금 파이썬 **0건** · 프런트 **0건**
- **HTTPS 미적용** — 외부 노출 시 필수
- **로그인 실패 잠금 없음** — 속도 제한(`rate_limit`)은 있으나 계정 잠금은 없음

---

## 3. 다음에 할 순서

1. ~~인증 DB 일원화~~ · ~~백업~~ · ~~덤프 암호화~~ · ~~의존성 감사~~ ·
   ~~정기 실행~~ · ~~훅 등록~~ 완료
2. **사용자가 해야 하는 것**: `schedule.ps1 install` 실행(관리자 권한),
   `BACKUP_KEY` 를 안전한 곳에 한 벌 더 보관(잃으면 백업도 잃는다)
3. `data/auth.json` 삭제 (DB 전환이 안정된 뒤)
4. 로그인 실패 잠금
5. 외부 배포를 실제로 할 때 — TLS·HTTPS·비밀 관리자 (2.3·2.4)
6. 이메일·소득 컬럼 암호화 (2.2)

---

## 4. 레퍼런스

| 주제 | 링크 |
|---|---|
| OWASP Password Storage (PBKDF2 600,000회 근거) | https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html |
| OWASP Database Security | https://cheatsheetseries.owasp.org/cheatsheets/Database_Security_Cheat_Sheet.html |
| PostgreSQL 행 수준 보안 (슈퍼유저 우회 명시) | https://www.postgresql.org/docs/current/ddl-rowsecurity.html |
| PostgreSQL SECURITY DEFINER 주의사항 (search_path) | https://www.postgresql.org/docs/current/sql-createfunction.html#SQL-CREATEFUNCTION-SECURITY |
| PostgreSQL TLS 연결 | https://www.postgresql.org/docs/current/libpq-ssl.html |
| pgcrypto | https://www.postgresql.org/docs/current/pgcrypto.html |
| CIS PostgreSQL Benchmark | https://www.cisecurity.org/benchmark/postgresql |
