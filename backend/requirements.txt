# --- Web framework ---
fastapi==0.141.1
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Data storage / processing ---
duckdb==1.1.3
pandas==2.2.3
numpy==2.2.1
pyarrow==25.0.1

# --- Market data sources ---
pykrx==1.0.51          # Korea: KOSPI/KOSDAQ OHLCV + fundamentals
yfinance==0.2.51       # US / global equities
finance-datareader==0.9.202  # Korea/US: 종목 리스트 + OHLCV (asset_detail)

# --- Optimization (portfolio) ---
scipy==1.15.0

# --- Utilities ---
python-dateutil==2.9.0.post0
httpx==0.28.1

# --- AI (개장 예측 서술 층; 키 없으면 규칙 기반만 동작) ---
anthropic>=0.40.0

# --- 가계부: 급여명세서 파싱(엑셀/PDF) + 파일 업로드 ---
openpyxl==3.1.5          # .xlsx 읽기 (pandas 엔진)
pypdf==6.16.1            # PDF 텍스트 추출
python-multipart==0.0.32 # FastAPI 파일 업로드
msoffcrypto-tool==6.0.0  # 카드사 메일 명세서: 암호 걸린 .xlsx/.xls 해제
cryptography==50.0.0     # 위 의존성 + pypdf 의 AES 암호 PDF 해제

# --- PostgreSQL (사용자 데이터·기준정보의 시스템 오브 레코드) ---
sqlalchemy==2.0.52
alembic==1.19.1
psycopg[binary]==3.3.4
